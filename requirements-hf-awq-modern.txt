# Install torch stack first from the CUDA 12.8 index:
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision torchaudio
#
# Then install the HF AWQ smoke dependencies:
#   pip install -r requirements-hf-awq-modern.txt
#
# torchao optional extension warnings are non-blocking if AwqMarlinLinear is
# selected and generation succeeds.
transformers
accelerate
safetensors
huggingface_hub[hf_xet]
gptqmodel
psutil
tqdm
